logging:
  level:
    org.springframework: INFO
    com.example: DEBUG

management:
  endpoints:
    web:
      exposure:
        include: health,info,refresh
  endpoint:
    health:
      show-details: always